# Run Gradle daemon on Java 21 — Kotlin Gradle DSL parser fails to parse
# the version string "25.0.2" (Kotlin bug fixed in 2.2+).
# The Java toolchain in build.gradle.kts still targets Java 25 for compilation.
# IMPORTANT: update this path to your local SDKMAN! Java 21 installation after cloning.
# Run `sdk list java` to find the installed path. Example: $HOME/.sdkman/candidates/java/21.0.10-tem
org.gradle.java.home=/Users/devbodegami/.sdkman/candidates/java/21.0.10-tem
org.gradle.daemon=true
org.gradle.parallel=true
